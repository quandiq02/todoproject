import {
    tabChecked,
    tabSettings,
    addLocalStorage,
    addNote,
    // sortCategory,
} from "./modules/index.js";

tabChecked();
tabSettings();
addLocalStorage();
addNote();
// sortCategory();